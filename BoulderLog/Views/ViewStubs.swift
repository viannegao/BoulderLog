import SwiftUI

// Temporary stubs — delete this file when RouteDetailView.swift and ImportFlowView.swift are added
struct RouteDetailView: View {
    let route: Route
    var body: some View { Text(route.name) }
}

struct ImportFlowView: View {
    var body: some View { Text("Import") }
}
